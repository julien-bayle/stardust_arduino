/* Pin definition (0 & 1 are used by TX/RX) */

const byte ULTRASOUND_ECHO_RIGHT = 11;	// HC-SR04 SENSOR
const byte ULTRASOUND_TRIG_RIGHT = 12;	// HC-SR04 SENSOR
const byte ULTRASOUND_ECHO_CENTER = 7;	// HC-SR04 SENSOR
const byte ULTRASOUND_TRIG_CENTER = 8;	// HC-SR04 SENSOR
const byte ULTRASOUND_ECHO_LEFT = 9;	// HC-SR04 SENSOR
const byte ULTRASOUND_TRIG_LEFT = 10;	// HC-SR04 SENSOR

const byte SERVOMOTOR_FRONT = 2;        // SERVOMOTOR
const byte SERVOMOTOR_LAUNCHER = 3;     // SERVOMOTOR

const byte SHARP_SIDE = 5;      	// SHARP IR SENSOR / ADC
const byte SHARP_LAUNCHER = 3;     	// SHARP IR SENSOR / ADC
const byte ILS_START = 4;     		// ILS / START
